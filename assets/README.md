# AI Router Skill - Assets

## Configuration

Copy `config-template.yaml` to `config.yaml` in your project root and customize settings.

## Examples

- `examples/routing.yaml` - Sample routing scenarios showing expected model selections
- `examples/CLI-usage.txt` - Command-line usage examples with expected output
